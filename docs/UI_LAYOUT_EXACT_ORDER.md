# VVChain exact UI ordering master

## Single source of truth
`assets/ui/master/UI_LAYOUT_MASTER_EXACT_ORDER.png`

The overall ordering and grouping must match the approved reference exactly.

## Full UI order
1. Top: one full-width `EQ / DYNAMIC EQ` graph.
2. Bottom strip from left to right:
   - BAND 1
   - BAND 2
   - BAND 3
   - BAND 4
   - MASTER

## Each BAND column, top to bottom
1. Band badge + `BAND n` + frequency class label.
2. `EQ` row: FREQ / GAIN / Q.
3. `DYNAMIC EQ` row: THRESH / ATTACK / RELEASE.
4. `UDMBC` row: DEGREE / LEVEL / MIX + ADV.
5. `ANALOG` row: TT / SS, AMOUNT, TYPE-A.
6. Bottom buttons: SOLO / BYPASS.

Do not rearrange these groups or substitute a different composition.

# GitHub Actions 檔案總覽

- `build.yml`：PR Fast Gate（含 whole-project static audit ×10）、main Windows VST3 incremental build、手動 Full Native / DSP Validation。
- `pages.yml`：GitHub Pages 的 docs-only 快速部署與 Web version/cache parity 檢查。
